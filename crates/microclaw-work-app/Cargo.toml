[package]
name = "microclaw-work-app"
version = "0.6.1"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
microclaw-core = { version = "0.6.1", path = "../microclaw-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
